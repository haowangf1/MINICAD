# MiniCAD（Qt + OCCT）

这是一个 Mini CAD 原型工程，基于 **Qt Widgets + OpenCascade（OCCT）**。

## 已完成
- 里程碑 A：Qt 窗口 + OCCT 视口（旋转/平移/缩放/FitAll）

## 构建（Windows / VSCode + CMake）

1) 安装 Qt（Qt6 或 Qt5）与 OCCT  
2) 在 VSCode 中配置 `CMAKE_PREFIX_PATH`（Qt）与 `OpenCASCADE_DIR`（OCCT）  
3) Configure / Build / Run



